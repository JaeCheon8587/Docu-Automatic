> **문서 맵**: [README](00.README.md) → [설계 결정사항](01.설계-결정사항.md) → [**아키텍처**](02.아키텍처.md) → [셋업 가이드](03.SETUP-GUIDE.md)

# Docusaurus 자동화 파이프라인 -- 상세 구현 계획 (v4)

## Context

`01.설계-결정사항.md`에서 확정된 4단계 사이클(`판단 -> md 작성 -> 검증 -> 저장`)을
Claude Code의 에이전트/스킬 체계로 구현하기 위한 상세 설계.

**핵심 설계 결정 (v4 확정):**
- **1단계 오케스트레이션**: Main CLI가 직접 테마 루프 + Agent 위임 + 재시도 + 저장을 수행
- 에이전트 3개 (scout + docu-writer + critic), 스킬 4개로 역할 분리
- 테마별 차이는 **scout가 작성하는 요구사항서의 내용**으로 구현
- **Full Reset 전략**: 매 테마마다 scout/docu-writer/critic을 새로 생성 (이전 에이전트 resume 금지)
- 검증 실패 시 **Main 내부**에서 docu-writer 재호출 제어 (최대 2회)

**v3 -> v4 변경 이유**: Claude Code에서 서브 에이전트가 서브 에이전트를 생성할 수 없음 (Agent 중첩 제약). v3의 3단계 중첩(Main -> task-orchestrator -> docu-writer/critic)을 v4의 1단계(Main -> scout/docu-writer/critic)로 평탄화.

**실행 환경**: Claude Code CLI
**실행 메커니즘**: Main CLI가 `skills/task-pipeline/SKILL.md`를 읽고, 테마마다 Agent 도구로 scout/docu-writer/critic을 생성하여 위임

**가능성 근거 (기존 스킬 실측 비교):**
- plan --consensus: 순차 15회 호출 (Planner->Architect->Critic x 5) -> 안정 동작
- team: 수십 회 병렬+순차 호출 -> 안정 동작
- 우리 파이프라인: 순차 14~28회, 테마 간 독립, 병렬 없음 -> **기존보다 구조가 단순**

---

## 1. 구성 요소 총괄

| 구성 요소 | 유형 | 파일 | 역할 |
|-----------|------|------|------|
| **task-pipeline** | 통합 오케스트레이션 스킬 | `skills/task-pipeline/SKILL.md` | 테마 루프 + 오케스트레이션 + Agent 위임 + 재시도 + 저장 |
| **scout** | 에이전트 | `agents/scout.md` | 코드베이스 탐색 + 판단 + 요구사항서 작성 (Level 1 리프) |
| **docu-writer** | 에이전트 + 스킬 | `agents/docu-writer.md` + `skills/docu-writer-skill/SKILL.md` | 범용 md 작성기 (Level 1 리프) |
| **critic** | 에이전트 + 스킬 | `agents/critic.md` + `skills/critic-skill/SKILL.md` | frontmatter + 테마 적합성 검증 (Level 1 리프) |
| **테마 정의** | 참조 데이터 | `skills/theme-definitions/SKILL.md` | 페이지 단위 테마 정의 (1차: 4개, 확장 예정) |

에이전트 수: **3개** (scout + docu-writer + critic)
스킬 수: **4개** (task-pipeline + docu-writer + critic + theme-definitions)

```
Main CLI (Level 0): skills/task-pipeline/SKILL.md를 읽고 실행
  |-> scout (Level 1 Agent): 탐색 + 판단 + 요구사항서 (리프)
  |-> docu-writer (Level 1 Agent): md 작성 (리프)
  |-> critic (Level 1 Agent): 독립 검증 (리프)
```

---

## 2. 테마별 실행 흐름

테마를 순차 순회. Main은 테마마다 Agent(scout)로 판단+요구사항서를 위임하고, Agent(docu-writer/critic)를 호출하여 작성/검증을 위임한 후, 결과를 저장한다.

**1차 스코프 (4개 테마)**:
`intro -> getting-started/requirements -> architecture/overview -> architecture/component-diagram`

> 2차 확장: 제품별 페이지, 라이브러리, 개발 가이드 등

```
[Main CLI -- skills/task-pipeline/SKILL.md]
  테마 루프: theme in [intro, getting-started/requirements, architecture/overview, architecture/component-diagram]

  +-------------------------------------------------------------------+
  | [테마별 4단계 처리 -- Main이 직접 수행]                               |
  |                                                                   |
  |  Step A. [판단 + 요구사항서 -- Agent(scout) 위임]                    |
  |    Agent 도구로 scout 에이전트 생성 (NEW)                           |
  |    (입력: theme, section, target, last_commit 등)                  |
  |    +-- scout가 내부에서 Glob/Grep + 판단 + 요구사항서 작성           |
  |    +-- proceed -> req_path, theme_def_path에 저장, status만 수신    |
  |    +-- skipped -> SKIPPED 처리, execution-log 기록, 다음 테마        |
  |    +-- fail -> FAIL 처리, consecutive_fail++                       |
  |                                                                   |
  |  Step B. [md 작성 -- Agent(docu-writer) 위임]                      |
  |    Agent 도구로 docu-writer 에이전트 생성 (NEW)                     |
  |    (입력: req_path + target + draft_path + (재시도 시) feedback_path)         |
  |    +-- draft_path에 md Write, status만 반환                        |
  |                                                                   |
  |  Step C. [검증 -- Agent(critic) 위임]                              |
  |    Agent 도구로 critic 에이전트 생성 (NEW)                          |
  |    (입력: draft_path + theme_def_path + req_path + feedback_path)   |
  |    +-- 3필드(result/frontmatter_valid/theme_fitness) 반환           |
  |    +-- fail 시 feedback_path에 피드백 Write                         |
  |                                                                   |
  |  Step D. [재시도 판정 + 저장]                                       |
  |    +-- Pass -> .md 파일 저장 + execution-log PASS 기록              |
  |    +-- Fail (1~2회차) -> Step B로 복귀                              |
  |    |     (critic 피드백 + 원본 요구사항서, 새 에이전트 생성)            |
  |    +-- Fail (2회 초과) -> auto_generated_warning 태그               |
  |           + .md 파일 저장 + execution-log WARNING 기록              |
  +-------------------------------------------------------------------+

  +-------------------------------------------------------------------+
  | [중단 판정]                                                         |
  |  PASS/SKIPPED/WARNING -> consecutive_fail 초기화                    |
  |  FAIL -> consecutive_fail++                                        |
  |  consecutive_fail >= 3 -> 파이프라인 중단                            |
  +-------------------------------------------------------------------+
```

---

## 2.5 Full Reset 전략

**정책**: 매 테마 처리 시 scout, docu-writer, critic을 모두 새로 생성한다. 이전 테마에서 생성된 에이전트를 재활용(resume)하지 않는다.

**근거:**

| 비교 항목 | Full Reset | Resume (재활용) |
|-----------|-----------|----------------|
| 컨텍스트 오염 | **없음** -- 매번 깨끗한 상태 | 있음 -- 이전 테마 잔여 컨텍스트가 판단 왜곡 가능 |
| 절차 준수 안정성 | **높음** -- 매번 스킬을 처음부터 읽음 | 낮음 -- 긴 컨텍스트에서 초기 지시 희석 가능 |
| 토큰 비용 (상대치) | **1.0x** (기준) | 1.8~3.6x (컨텍스트 누적으로 매 호출 토큰 증가) |
| 디버깅 용이성 | **높음** -- 테마별 독립 실행, 문제 격리 쉬움 | 낮음 -- 이전 테마와의 상호작용 추적 필요 |
| 구현 복잡도 | **낮음** -- 단순 생성/폐기 | 높음 -- 상태 관리, 컨텍스트 정리 로직 필요 |

**구현 방식:**
- Main이 `Agent` 도구로 scout/docu-writer/critic을 호출할 때마다 새 서브 에이전트를 생성
- 재시도 시에도 scout, docu-writer, critic을 새로 생성 (이전 에이전트 resume 금지)
- 이전 테마의 결과가 필요한 경우, 파일 경로를 전달하여 에이전트가 직접 읽도록 함 (컨텍스트 전달이 아닌 파일 시스템 경유)

---

## 3. 각 구성 요소 상세

### 3.1 task-pipeline (통합 오케스트레이션 스킬 -- Full Template)

**역할**: Main CLI의 전체 행동을 정의하는 유일한 오케스트레이션 스킬. **테마 루프 + 판단 + 요구사항서 + Agent 위임 + 재시도 + 저장**을 통합.

**실행 메커니즘**: Main CLI가 이 스킬을 읽고, 테마마다 Agent 도구로 scout(판단/요구사항서) → docu-writer(작성) → critic(검증)을 순차 생성하여 위임

**v4 변경 -- 역할 흡수:**
- v3의 task-pipeline 스킬(루프+저장)과 task-orchestrator 스킬(판단+요구사항서+재시도)을 통합
- Main이 직접 수행: 재시도 제어(Step D), 저장(Step D), 테마 루프, 상태 추적
- Agent 위임: 판단+요구사항서(Step A -> scout), md 작성(Step B -> docu-writer), 검증(Step C -> critic)

**CRITICAL 제약 8개:**
1. .md 내용 직접 작성 금지 -> Agent(docu-writer)에 위임
2. 검증 직접 수행 금지 -> Agent(critic)에 위임
3. docu-writer 결과 수신 전 critic 호출 금지 (순차)
4. 테마 순회 반드시 순차 (병렬 금지)
5. 테마마다 새 scout/docu-writer/critic 생성 (Full Reset)
6. 재시도 최대 2회 Hard Cap
7. 3연속 FAIL 시 파이프라인 중단
8. Step A의 Glob/Grep을 Main이 직접 수행 금지 → Agent(scout)에 위임

**안정성 장치:**
1. **execution-log.md** (파일 기반 상태 추적): YAML 상태 헤더(current_theme_index, consecutive_fail, completed_themes) + 결과 테이블. 매 테마 시작 전 Read로 상태 복구, 완료 후 Write로 업데이트. 오토 컴팩트 시 변수 소실 대비.
2. **순차 대기 게이트**: Execution_Policy에 순차 제약을 CRITICAL로 명시
3. **반복 상한**: 3개 테마 연속 FAIL 시 파이프라인 중단

### 3.2 docu-writer (에이전트 + 스킬)

**역할**: Main의 요구사항서를 받아 .md 파일 내용 작성

**입력** (4개 입력 계약):
1. **req_path** (항상 전달): 요구사항서 파일 경로 — Read로 로드하여 14필드 파싱
2. **target** (항상 전달): 대상 코드베이스 경로 — docu-writer가 related_files를 이 경로 기준으로 직접 Read/Grep/Glob
3. **draft_path** (항상 전달): 작성한 md를 Write할 파일 경로
4. **feedback_path** (재시도 시만 전달): critic 피드백 파일 경로 — Read로 로드

**출력**: 경량 확인서 `{ status, draft_path }`. md는 draft_path에 Write 저장.

**스킬 적용**: Lite Template
- 핵심 규칙: "요구사항서의 perspective와 writing_style을 엄격히 따를 것"
- "do_not_cover에 명시된 내용은 절대 포함하지 말 것"

### 3.3 critic (에이전트 + 스킬)

**역할**: docu-writer 결과물을 검증

**입력** (4개 입력 계약):
1. **draft_path** (항상): docu-writer가 작성한 .md 파일 경로 — Read로 로드
2. **theme_def_path** (항상): 테마 정의 파일 경로 — Read로 로드
3. **req_path** (항상): 요구사항서 파일 경로 — Read로 로드
4. **feedback_path** (항상): critic 피드백 저장 경로 — fail 시 Write

**출력 (Main에 반환 — 3필드)**:
```yaml
result: pass  # 또는 fail
frontmatter_valid: true
theme_fitness: pass  # 또는 fail
```

**출력 (feedback_path에 Write — fail 시만)**:
```yaml
critic_feedback:
  - "tech-spec 영역의 구현 세부사항이 혼입됨 (라인 23-31)"
  - "sidebar_position 필드 누락"
```

**검증 기준 (2단계)**:
1. **frontmatter 검증** (규칙 기반, 7개 체크):
   - YAML frontmatter 블록(`--- ... ---`) 존재 여부
   - 9개 필수 필드 모두 존재 (title, sidebar_label, sidebar_position, section, theme, auto_generated, source_files, last_commit, generated_at)
   - `sidebar_position`이 숫자 타입
   - `theme`이 theme-definitions에 정의된 테마 ID 중 하나
   - `auto_generated`가 true
   - `source_files`가 비어있지 않음 (최소 1개)
   - `generated_at`이 ISO-8601 형식
2. **테마 적합성 검증** (AI 판단, 5개 관점):
   - perspective 일치: 문서 관점이 테마 정의와 부합하는가
   - do_not_cover 위반 스캔: 금지 내용이 혼입되지 않았는가 (라인 번호 기록)
   - must_cover 완전성: 필수 항목이 모두 다뤄졌는가
   - audience 적합성: 용어 수준과 설명 깊이가 대상 독자에 적합한가
   - writing_style 부합: 서술형/참조형 등 요구 스타일을 따르는가

**스킬 적용**: Lite Template

### 3.4 theme-definitions (참조 데이터)

**역할**: Main(task-pipeline 스킬)과 critic이 참조하는 페이지 단위 테마 정의

**내용**: 실제 Docusaurus 사이트 구조(http://110.110.10.70:8080/) 기반으로,
각 테마의 관점/대상 독자/작성 스타일/포함 범위/제외 범위를 구조화.
1차 스코프: intro, getting-started/requirements, architecture/overview, architecture/component-diagram (4개)

---

## 4. 산출물 목록 (구현 시 작성할 파일)

| # | 파일 | 설명 | 템플릿 | 상태 |
|---|------|------|--------|------|
| 1 | `skills/task-pipeline/SKILL.md` | 통합 오케스트레이션 스킬 | Full Template | **완료** |
| 2 | `skills/docu-writer-skill/SKILL.md` | docu-writer 스킬 | Lite Template | **완료** |
| 3 | `skills/critic-skill/SKILL.md` | critic 스킬 | Lite Template | **완료** |
| 4 | `skills/theme-definitions/SKILL.md` | 페이지 단위 테마 정의 (1차: 4개) | -- | **완료** |
| 5 | `agents/docu-writer.md` | docu-writer 에이전트 정의 | -- | **완료** |
| 6 | `agents/critic.md` | critic 에이전트 정의 | -- | **완료** |
| 7 | `agents/scout.md` | scout 에이전트 정의 | -- | **완료** |
| 8 | `skills/task-pipeline/SKILL-REFERENCE.md` | Examples/Failure_Modes/Tool_Usage 참조 | -- | **완료** |

---

## 5. I/O 계약

| # | 송신 → 수신 | 데이터 | Main 컨텍스트 비용 |
|---|------------|--------|:---:|
| 0 | Main 자체 조달 | last_commit (Bash), generated_at (Bash), execution-log 상태 (Read) | ~20토큰 |
| 1 | Main → scout | theme, section, target, last_commit, generated_at, output_path, draft_path, **req_path, theme_def_path** | ~80토큰 |
| 2 | scout → 파일 | requirement_spec → req_path, theme_definition → theme_def_path | 0 |
| 2' | scout → Main | **status만** ("proceed" / "skipped" + reason / "fail" + reason) | ~10토큰 |
| 3 | Main → docu-writer | **req_path, target, draft_path, (retry: feedback_path)** — 경로만 | ~50토큰 |
| 4 | docu-writer → Main | 경량 확인서 `{ status, draft_path }` (~50 tokens). md는 draft_path에 Write 저장. | ~10토큰 |
| 5 | Main → critic | **draft_path, theme_def_path, req_path, feedback_path** — 경로만 | ~50토큰 |
| 5' | critic → 파일 | critic_feedback → feedback_path (fail 시만) | 0 |
| 6 | critic → Main | **result/frontmatter_valid/theme_fitness만** (3필드) | ~20토큰 |

**교차 검증**: #3 입력 = docu-writer Input_Contract (경로 기반), #5 입력 = critic Input_Contract (경로 기반).

---

## 6. 검증 방법

1. task-pipeline/SKILL.md의 `<Steps>`가 Phase 1~3 + Step A~D를 모두 포함하는지 확인
2. **CRITICAL 제약 8개** 모두 `<Execution_Policy>`에 명시되는지 확인
3. **I/O contract 교차 검증**:
   - Main -> docu-writer 전달 = docu-writer Input_Contract (요구사항서 + target 경로 + 피드백)
   - Main -> critic 전달 = critic Input_Contract (md 내용 + theme_definition + 요구사항서)
4. Agent 호출 리터럴 템플릿이 docu-writer.md, critic.md의 에이전트 정의와 일치하는지 확인
5. **Full Reset** 명시: task-pipeline/SKILL.md의 Execution_Policy에 CRITICAL 5로 기술
6. theme-definitions/SKILL.md의 테마가 실제 사이트 URL과 1:1 매핑되는지 확인
7. execution-log.md 기반 상태 추적이 task-pipeline/SKILL.md에 반영되는지 확인
8. 삭제된 파일(task-pipeline.md, task-orchestrator.md, task-orchestrator-skill/SKILL.md) 참조가 다른 파일에 남아있지 않은지 확인
